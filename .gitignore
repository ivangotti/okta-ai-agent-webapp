# Dependencies
node_modules/

# Agent Keys (sensitive)
agent-keys/

# Logs
*.log
npm-debug.log*

# Environment (NEVER commit these!)
.env
.env.local
.env.*.local
.env.production

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Temp files
/tmp/
*.tmp

# Nested lockfiles (only the root lockfile is tracked)
nist-mcp-server/package-lock.json
users-mcp-server/package-lock.json

# Generated DB / seed artifacts (regenerate via each server's setup script)
*.db
nist-mcp-server/comprehensive-question-bank.json
